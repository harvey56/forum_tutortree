import { KeyboardArrowUp, KeyboardArrowDown } from './';
import styled from 'styled-components';

export const StyledKeyboardArrowUp = styled(KeyboardArrowUp)`
  &:hover {
    cursor: pointer;
  }
  margin-left: 0 auto;
`;

export const StyledKeyboardArrowDown = styled(KeyboardArrowDown)`
  &:hover {
    cursor: pointer;
  }
  margin-left: 0 auto;
`;
